# Repo Notes

This repo is meant to be updated whenever:

- a new X extraction path is validated
- a current fallback breaks
- OpenTwitter or OpenNews changes behavior
- `bian` wrappers are improved

Suggested future additions:

- sample redacted responses
- DOM extraction implementation notes
- final production intake flow diagram
